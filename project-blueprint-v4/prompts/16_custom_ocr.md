Implement Custom OCR flow.

Requirements:
- route when template_not_found
- page navigation
- draw ROI per page
- OCR custom ROI only
- result grouped by page
- optional Request Template from custom ROI

Stop after this phase.
